%html
  %head
    %title
      = yield(:title).empty? ? 'TMS' : "TMS | #{yield(:title)}"
    = stylesheet_link_tag 'home'
    = csrf_meta_tags
    %meta{:charset => "utf-8"}/
    %meta{:content => "width=device-width, initial-scale=1.0", :name => "viewport"}/
    %link{:href => "http://fonts.googleapis.com/css?family=Open+Sans", :rel => "stylesheet", :type => "text/css"}/
    %link{href: asset_path('fav.ico'), rel: "shortcut icon"}
  %body
    #container-fluid
      = render :partial => '/layouts/home/header'
      -#.flash.bg-danger
      -#  - flash.each do |type, message|
      -#    %div{:class => " alert alert-#{type}"}
      -#      %span= message
      -#      %span.pull-right.x{style: "cursor:pointer; padding-right: 20px"} X
      #main-wrap.main-color
        = yield
    = render :partial => '/layouts/home/footer'
    = javascript_include_tag 'home'
